# STANDARD TEMPLATE CONSTRUCT (STC)

> **This is STC `Mk II Mod 2 A1`.** Mk I is preserved in `ARCH/` and is not superseded retroactively — a project pinned to `Mk I Mod 7 A0` stays correct against Mk I until its owner decides otherwise, as a Mod bump of that project. What changed and why: the Standard Revision History at the foot of this file.
>
> *(Numbering note: a Mark's baseline is **Mod 0**. Mk I began at Mod 1 before this was settled and is left as written — a revision history that gets retrofitted stops being one.)*

**What this is:** a project-agnostic standard for how a software project's *design workspace* is organised — document set, section skeleton, versioning scheme, and the directory architecture of the thing you eventually build. It keeps only the *shape* of a well-run project's documentation, deliberately stripped of any domain-specific decision. It applies to any project — services, games, embedded firmware, CLIs, data pipelines — not only AI agents.

**Why it exists:** so every project you start has documentation in the same place, in the same order, following the same rules — instead of each one reinventing where the spec lives, how build steps are sequenced, or how "done" is defined. A contributor (human or AI coding agent) who has read one STC project already knows the shape of the next one.

**Why "Railroad" and not "Roadmap".** A roadmap suggests a route with junctions you may take differently on the day. This document is the opposite: the track is laid first, and the code generator rides it without deciding where to turn. Every decision is made upstream in `Logic.md`, frozen into signatures and pinned parameters, then sequenced into steps whose bodies are close to mechanical. The name is the contract.

---

## Scope: what STC standardises, and what it deliberately does not

STC fixes **how** a project is specified and built — the four documents, the interface freeze, the railroad, the verification altitude, the version scheme. It does **not** prescribe **what kind of system** you are building.

That separation is enforced by two mechanisms. The first is the **Project Profile** (`Logic Template.md` §0.1): twelve numbered yes/no rows — fifteen answers, since three of them split in two where one question was found to be carrying two different obligations. They ask about any dependency outside the process, two or more external surfaces, persistent state, a second store, an evolving core, operator-selected modes, self-detected modes, concurrency within a run, work on its own schedule, unattended survival, interchangeable backends, plugins, a real trust boundary, correctness that cannot be asserted exactly, and an adopted framework. **Every row defaults to NO.** Each NO names exactly which sections, appendices and directory blocks are **deleted** from that project's documents. The second is the **Pattern** (below), computed from those same answers, which decides how much of the standard's apparatus runs at all.

- Answer all NO and you get the **floor**: entry point, config, the work, its utilities, its data files, logs — plus a short logic doc and a short railroad. A single-purpose script or CLI tool looks exactly like this, and it should be *small*. **A ten-page logic doc for a three-file program is a failure of this template, not a success.**
- Answer some YES and the corresponding structures reappear, up to a multi-surface, multi-store, plugin-extensible system.

Deleting an inapplicable section is the standard working correctly — not cutting corners. The reverse is the expensive error: a data-layer facade over one CSV file, or a pipeline abstraction with a single stage, is indirection every future step must route through for no benefit. Turning a NO into a YES later is a **Mod bump** — a recorded, deliberate growth, and cheaper than carrying scaffolding nobody uses.

### How the standard scales: the process is constant, the content is not

STC is meant to carry a fifty-line utility and a system of a hundred modules **without changing shape**. That works because the two are separated:

| Constant, whatever the project | Scales with the project |
|---|---|
| four documents | length of `Logic.md` §3.3 — one subsection per module, each as deep as its decisions genuinely are |
| the profile's rows, and the three Patterns | how many rows are YES, therefore how many concern sections survive, and which Pattern the project computes to |
| the seven rails and the step template | number of chapters, and number of steps per chapter |
| two verification artifacts per chapter — the checklist and the evidence artifact | number of entries in Appendix B.8 / C, and the length of the end-to-end flows |
| step size — always close to mechanical | total step count |

**Detail is depth, not layers.** A very complex system earns a very long logic doc: twenty-plus module subsections in §3.3, sub-flows numbered inside them, case tables, worked examples, a long interface freeze. What it does *not* earn is architecture nobody needs — extra indirection is not thoroughness, and a section describing a structure that was never built is worse than no section.

**A large railroad is expected, and harmless by construction.** More work means *more steps*, never bigger ones (rail 5). The reading order in §0 means a Builder loads one step plus the appendices it names — never the whole file — so a railroad of three hundred steps costs the same per step as one of twenty. Size is a navigation problem, and the numbering is the navigation.

**Under-specification is the expensive failure, not length.** Where a module's logic is written thinly, a Builder fills the gap by inventing — which is the one outcome every rail exists to prevent. Brevity is never the goal; *every paragraph carrying a decision or a reason* is.

---

## Patterns — the complexity class, and why the standard needs one

The profile scales **breadth**: which structures exist. It does nothing about **ceremony** — how much apparatus is brought to bear on them. Without a second dial, a three-file tool and a system with every row YES run the identical process: full contract appendices, a chapter ladder, per-step decision lists, a per-chapter acceptance walk, an audit protocol. That is how a standard silently raises its own floor, and it is the failure this section exists to prevent.

**Patterns are derived from solutions, not declared for projects.** Each mechanism in this standard was placed by asking *at what minimum profile does this start paying for itself?* — and the class boundaries are where many mechanisms turned out to demand the same rows. The count was not chosen in advance; two clusters emerged above the floor, so there are three classes. (The word *tier* is deliberately avoided: this standard already uses it for an architectural layer, in the Mod-bump rule.)

**The class is computed, never chosen — and the rule lives in one place: `Logic Template.md` §0.1.** Three classes: **I** the floor (one process, its own disk, a human running it) · **II** a boundary exists (a service, a store, a provider, an extension, an untrusted party, overlapping work — failures are now partial and foreign, but somebody is watching) · **III** nobody is watching (the system degrades or schedules itself, and is expected to survive without a human present). The build-side apparatus each class runs is catalogued, numbered, in `Railroad Template.md` §0. **This section explains why the mechanism exists; it does not restate it.**

**Two profile rows deliberately never raise the class:** the one about correctness that cannot be asserted exactly, and the one about an adopted framework. Each switches on exactly one contract and says nothing about whether a boundary exists or whether anyone is watching. A locally-run, attended script whose output is *judged* rather than asserted is a Pattern I project and should read like one. Letting a technology — or a correctness shape — set the ceremony is precisely how a standard inherits the identity of whatever system it was extracted from.

Six properties the rule was built to satisfy, each of which rules out a tempting shortcut:

| Property | What it forbids |
|---|---|
| **Total** | every one of the 2¹² profiles resolves; no combination needs a human to arbitrate |
| **Deterministic** | no predicate requiring judgement — no "the project is complex", no "a significant number of modules" |
| **Monotone** | flipping any row NO→YES never *lowers* the class; a rule that relaxes rigour when a requirement is added is broken however well it scores on examples |
| **Technology-independent** | the same problem in two ecosystems computes the same class; no weight is a function of the stack |
| **Volume-independent** | no count of lines, files or modules enters the rule — volume is a *consequence* of complexity, not a measure of it (*detail is depth, not layers*) |
| **Floor-dominant** | the common tool must land in Pattern I; a floor nobody would use for a small tool is a dead letter, not a floor |

### The boundary between what a class may and may not touch

**Immovable at every Pattern:** the document set, the rails, the step template, human-walked chapter-altitude verification, and the freeze. **Movable by class:** the numbered apparatus catalogue in `Railroad Template.md` §0 — separate versus inline contract appendices (A1), the audit protocol (A2), and the per-chapter evidence artifact (A3). **A4 was the separate Pattern III probe and is retired in Mk II, folded into A3**; its number is kept and never reused, because `PATTERN-EXCESS` counts against these numbers and recycling one would silently invalidate every count already recorded.

The line sits there because the immovable set answers *what did this project decide, and how do we know it works* — the properties every STC project is supposed to share, and the reason a standard exists at all. Everything movable is **apparatus**: it changes how much machinery proves the same thing, never what is proven. The test for anything proposed for the movable list: *does changing it alter what a reader can learn about the system, or only how long it took to write down?* Only the second may move.

**Which sections exist is not the class's business — it is the profile's.** An earlier revision of this section listed sections per class ("Pattern I forbids §4, §5, §10…"), which was true but derived: §4 is gated by the surfaces row, and that row is itself a class trigger, so the prohibition was a second copy of a rule the profile already carries — two places to change, and a guaranteed drift. The class governs apparatus; the profile governs structure; the over-engineering prohibitions that belong to neither (a second entry point with no second reason, a role-grouped directory holding two things, an adapter in front of one surface, a facade over one file) live in `Logic Template.md` §2.1, where the rest of the tree's rules are.

### Where a class cannot help, and the one artifact that fills the gap

**A human walk cannot see a race, a lost write, a duplicated external effect after a restart, or a loop that is alive but starved.** No amount of checklist discipline fixes this: those failures do not appear on the path a person follows. That is a real hole in chapter-altitude verification, and it is exactly the hole Pattern III projects fall into.

The answer is **not** to reintroduce a per-step suite, and not to declare a happy-path re-walk sufficient — it walks the same path the human just walked. (Nor is the answer to avoid anything test-*shaped*: the artifact below is executable and looks like a test, and what makes it legitimate is its altitude and whose criteria it serves — see the three clauses in `Railroad Template.md` §0.) It is the **provocation job of the chapter's evidence artifact** (apparatus A3, `Railroad Template.md` R3.3): two concurrent writers, then count the records; kill mid-effect, then assert the effect happened once; hold the loop busy, then assert the beat still advances. One artifact, chapter altitude, and — the constraint that keeps rail 2 intact — **every provocation traces to a `V#` the Architect wrote**, so the Builder decides how the property is provoked and never which property matters.

> **Mk I made this a separate artifact (A4) and Mk II folded it into A3.** Splitting them cost a second write event and bought no second kind of proof: both files boot the same system, both are read by the same person at the same gate, and both are stale the moment the criteria move. The failures listed above are still exactly what a human walk cannot see — that argument was right and is unchanged. What was wrong was believing it needed its own file.

### Crossing a class boundary is a Mod, not a new Mark

A class crossing is a **Mod bump**; `Mk` is reserved for a change of *purpose*, which is defined independently of complexity: **a `Mk` bump is warranted when the end-to-end flow in Appendix D is *replaced* rather than *extended*** — when "what does running this do?" gets a different answer, not a larger one. Adding a surface, a store, a provider or unattended operation extends the flow; turning a report generator into an interactive service replaces it. Put the old Appendix D beside the new one: if the old flow still runs, unchanged in intent, it is a Mod.

The reasoning is not aesthetic. Treating a class crossing as a new Mark would discard the Revision History — the project's only defence against reconstructing decisions from memory — and, worse, it would make honest profile answers costly: if raising a row can end the project's identity, the profile stops being answered truthfully, and a mechanism that rewards lying on its own input destroys itself.

### The four ways out, kept deliberately separate

**`PATTERN-WAIVER`** — omitting something your class requires: recorded in `Logic.md` §14 with **what covers the risk instead**, because a waiver naming no compensation is a shrug with a reference number. Uncounted, non-blocking, re-read at every Mod. **`PATTERN-EXCESS`** — adopting one **numbered** apparatus item from the class above: allowed three times, counted against the catalogue in `Railroad Template.md` §0 so that "one structure" is enumerable rather than arguable; the third means the class is wrong, so the project is promoted as a Mod. **`STC-GAP`** — the standard has no rubric for a decision already made: **do not halt**, record it in `Logic.md` §14, and copy it to the register below. **Version pinning** — every `Logic.md` declares `Built against STC Mk … Mod … A…`.

The asymmetry between `STC-GAP` and `CONTRACT-GAP` is deliberate and worth stating plainly: a `CONTRACT-GAP` means the decision itself is missing, so improvising produces the wrong program and the Builder must stop. An `STC-GAP` means the decision exists and only its *form* is missing — halting there would charge a project for the standard's incompleteness.

#### `STC-GAP` register — the one piece of state the standard keeps across projects

The three-projects rule cannot enforce itself from inside the projects: entries scattered across three `Logic.md` §14 sections never meet, so nobody ever notices the third. It therefore needs a home here, and that home is a genuine concession — the four documents are per project and the standard is otherwise templates only, so this register is the sole mutable, cross-project artifact STC owns. It is small on purpose: one line per gap, one increment per project that hits it.

| Gap (what the standard had no rubric for) | Projects that hit it | Outcome |
|---|---|---|
| What justifies a **`Mk` bump of the standard itself**? STC defined `Mod` and `A` for itself and `Mk` only for projects, so a proposal to start a new Mark of STC had no rubric to be judged against. | `AID4` | **adopted in Mk II** — a `Mk` bump of the standard is warranted when what a project must *produce* changes, or the altitude at which it is *proven* changes. See the versioning section above. |
| Where does a project record the **observation of a pinned mechanism** — a threshold, a refill rate, an ordered effect trace — when rail 6 requires it exact and the human walk cannot see it? | `AID4` | **adopted in Mk II** — job 2 of the chapter evidence artifact (A3, `Railroad Template.md` R3.3). Before this the need was real and unhoused, so the project grew per-step evidence files, breaching the artifact cap for a legitimate reason. |
| `<one line>` | `<project A, project B>` | `<open · adopted in Mod N · rejected because …>` |

**A rejection is written down once, with its reason, and that is the point.** An unrecorded rejection is re-proposed by the next project, and the standard re-litigates a settled question every time it is used.

---

## The four documents (design workspace)

**Four documents are law. Two more exist, and neither is.** The law is written *from* something and executed *with* something, and pretending otherwise is how both of those turn into folklore:

| Artifact | Phase | Who writes it | Standing |
|---|---|---|---|
| **`Specification.md`** | **before** the law | the **owner**, alone, in plain language (English template or Polish twin — pick one) | an **input**. Frozen and moved to `ARCH/` the day `Logic.md` opens. Never consulted to build anything; its only later job is to answer *"how far have we drifted from what was originally asked for?"* |

**What the specification is shaped like, and why it is not a questionnaire.** It asks for a premise, **a nested tree of the parts** (required: that tree is how the structure is introduced in the text, not an optional sketch), then — the part that carries the most — **plain-language walkthroughs of how they imagine each part working**, branches included, as long as they need to be, and only then a numbered list of what they want. **That order is deliberate.** The walkthrough is what most owners actually produce first when left alone, and a template that has no room for it gets one written anyway, in the margins of something else; the feature list is an index, and an index is written last — by reading the walkthroughs back and naming what each one delivers. Every `F#` names the walkthrough that delivers it, which makes coverage a property of the writing order rather than a check somebody has to remember. The **premise is the reference point**: every item is checked against it, and a divergence between the two is the earliest visible sign that a project is going wrong — cheap to fix there, a Mod bump once the law is written, a rebuild once code exists. The mapping from the owner's answers onto profile rows lives in `Logic Template.md` §0, not in the specification: the owner answers plain questions and never has to learn what a profile row is.
| the four below | the law | the Architect, with the owner | **normative** |
| **`manuals/DevLog.md`** | **during** the build | the **Builder**, every session | a **journal**. Never law, never a decision, never evidence for a `V#`. Lives in the built project's repository, because its whole job is continuity across sessions and machines |

Neither of the two is a fifth or sixth "document" in the sense the next table means, and neither may drift into one: a specification that keeps being edited after the freeze stops being a record of intent, and a dev log that accumulates decisions becomes a shadow standard nobody froze. Both templates state their own hard rules; the rules are the point of them.

The design workspace itself holds exactly four files, flat, no subfolders:

| File | Role | Analogous to |
|---|---|---|
| **`Logic.md`** | the **law** — *what* the system does and **why**: principles, the file architecture, the application's own logic module by module, then contracts and frozen interfaces. No implementation code. | a constitution / RFC |
| **`Railroad.md`** | the **build order** — sequences construction of the thing `Logic.md` describes into small steps, gated by acceptance criteria. Adds no new decisions of its own. | a project plan with acceptance criteria baked in |
| **`dependency.md`** | everything **outside** the package manager: external tools/runtimes, git-sourced or vendored repos, runtime-downloaded artifacts. | an infra/ops manifest |
| **`requirements.<ext>`** | the pinned, resolved package-manager manifest (`requirements.txt`, `package.json`, `Cargo.toml`, …). | `package-lock.json` / `poetry.lock` |

**This workspace is design-only.** It is not the built project's repository — it may not even be a git repo. The tree in `Logic.md` §2.1 is the *target* project's layout, built elsewhere (or in a subfolder / sibling repo), not a description of the folder these four files live in.

### The division of labour between the two main documents

This is the distinction the whole standard rests on, and the one most often blurred:

| | `Logic.md` | `Railroad.md` |
|---|---|---|
| Answers | **WHY** it is built this way, and **WHAT** it decides | **WHAT to generate, step by step** |
| Contains | the reasoning, the rules of the problem, the file architecture, the module-by-module logic, the frozen contracts | ordered steps, verbatim signatures to implement, MUST-NOTs, acceptance criteria |
| Never contains | implementation code, ready-to-paste files, prompts for a code generator | a new decision of any kind |
| Read by | a human reviewer first, an agent second | the Builder, one step at a time |

**`Logic.md` §3 (Application Logic) is the longest section of the standard for a reason** — it is the business analysis: the domain vocabulary, the rules with their stated goals, one logic subsection per module (purpose, what it is *not* responsible for, its sequence, its decisions **and the reasoning behind each**, its failure behaviour), and the end-to-end flows. A document of contracts alone specifies a shape nobody can evaluate: a Builder can satisfy every signature and still write the wrong program, because "wrong" is only definable against intent.

**The file architecture lives in `Logic.md` §2.1, not in the railroad.** Where a responsibility lives is a design decision with a why — that is what keeps a project modular — so it belongs in the law. `Railroad.md` §0.5 only annotates the same tree with stub and build-order rules; if the two disagree, the logic doc wins and the difference is a `CONTRACT-GAP`.

**One bin for everything disposable.** Spikes, probes, one-off scripts, sample data, temporary output — all of it goes in `directorium_temporarium/` and nowhere else. It is gitignored, product code never imports from it, and it is **emptied at every chapter gate**. Anything in it that turned out to matter gets promoted into the real tree, by a step that owns it, *before* the bin is emptied. The project root stays clean.

**Naming convention:** `Logic.md` may carry a version suffix in its filename once versioning starts (see below), e.g. `Logic Mk I Mod 1 A1.md`; superseded versions move to an `ARCH/` (archive) subfolder rather than being deleted, so revision history is never fabricated from memory.

---

## The versioning scheme (Mark / Mod / A)

`Logic.md` is a living document, so it needs a version scheme that says *how much changed*, not just *that* something changed:

- **Mark (`Mk`)** — the project's fundamental generation. Fixed once chosen; changing it means starting a new project, not revising this one.
- **Mod (Modification)** — bumped on a **fundamental change in logic**: architecture, a tier being added/removed, a core invariant reversed.
- **A (Alteration)** — bumped on a **smaller change within the same logic**: a clarification, a missing contract filled in, a parameter pinned, a contradiction resolved.

Every version bump is recorded in `Logic.md`'s **Document Revision History** (own section, append-only, oldest first) — never overwritten, never silently squashed. A bump's entry states *what changed and why*, not just *that* it changed; "why" is what lets a future reader judge whether a since-surfaced problem is already covered.

**Rule of thumb:** if the change would make an already-approved `Railroad.md` step wrong, it's at least a Mod. If it only fills a gap the railroad already assumed was filled, it's an A.

### The standard versions itself, and every project pins the version it was built against

STC applies the same scheme to itself: **Mod** for a structural change (a rail, an invariant, a section, a Pattern rule), **A** for a clarification that changes nothing structural. Every bump is recorded below, append-only, oldest first.

**And a `Mk` bump for the standard itself — defined in Mk II, because Mk I never was.** Mk I gave itself Mod and A and defined `Mk` only *for projects* (Appendix D's flow replaced rather than extended), so the question "what would justify a new Mark of STC?" had no rubric anywhere in the standard — the first genuine `STC-GAP` about STC, now registered below. The rule: **a `Mk` bump of the standard is warranted when what a project must PRODUCE changes, or when the altitude at which it is PROVEN changes.** A rail, a section, a Pattern rule, an appendix — those reshape how the work is done and are Mods however large they read. The artifact set and the verification altitude are what a reader of any STC project can see from the outside, and changing either means projects written before and after are no longer the same kind of object. That is a Mark.

Every project declares in `Logic.md` §0: `Built against STC Mk II Mod 2 A1`. This is not bookkeeping. Without it, "the project follows STC" means "it follows whatever STC happens to be today", and every later edit to the standard silently invalidates every document written before it — a moving target that no project can be audited against. **A change to STC is never retroactive.** Migrating a project onto a newer version of the standard is a decision its owner takes deliberately and records as a Mod bump of that project.

#### Standard Revision History

**Mk II**

* **Mk II Mod 2 A1** — *the specification's section titles and its failure label moved into a formal register; no rule changed.* The owner reworded four headings after Mod 2 landed, and the pass exposed three places where a rename had not travelled and two titles that said less than the ones they replaced. **What was reworded:** §7 *How it lives in the world* → **Target Rules of Engagement**, §8 *What happens today* → **Current status at the time of writing the documentation**, §11 *What comes later* → **Further development plans**, and §4's per-part failure line from *what should happen when this goes wrong* to **In case of failure or error**. **Three stale quotations, each a rename that did not travel.** §12's readiness list quoted the old failure label in both twins, and `Logic Template.md`'s handover map cited both `§4's "what should happen when this goes wrong"` and `§11 "what comes later"`. A table whose left column is quotations of another document's headings is exactly the artifact a rename breaks silently — nothing about it stops working, it just stops pointing at anything — which is worth naming here because the handover map is the one place where the specification and the law are held against each other line by line. **Two titles were narrowed and have been widened back.** §7 was first retitled *…for production*, which excluded the section's own seventh question — where the thing will be **built**, which is not production, lands in `manuals/DevLog.md`'s machine-notes table, and has a closing callout in that same section explaining why it sits there at all; the qualifier is dropped, and the section is again titled by what it collects rather than by where the thing runs. §11 lost *a heads-up, not a request*, and that clause is restored: it is the section's first line of defence against being read as an order and built, which is the entire reason `Mk I Mod 6 A0` gave the section rules rather than just a home, and leaving that job to the callout underneath is strictly weaker than having it in the heading a skimming reader sees. Its replacement, *outside the scope of this document*, was also imprecise in the new register — the content is in the document; what stands outside is the **ask**. **One register fix, Polish only, with a twin-drift underneath it:** the Polish twin read *porażka* — a defeat, not a fault — and had drifted to two nouns where the English carried one, in the one label the handover map quotes. Both now read *awaria lub błąd* / *failure or error*. **Deliberately NOT changed:** the section order, the `F#` block's three fields, §12's coverage check, and the English idiom in the Polish §7 heading — §0's plain-language rule governs what the owner writes, not what the standard calls its own sections.
* **Mk II Mod 2 A0** — *the specification's feature list moved behind the walkthroughs, and stripped of the two fields that let an owner hedge.* Four changes to `Specification Template.md` and its Polish twin, all pulling the same way. **The order.** The numbered `F#` list and the walkthroughs swap places, becoming **§4 How it should work** and **§5 What it should do**. This finishes a decision `Mk I Mod 4 A1` had already half-made: that bump demoted the `F#` items to "a short numbered index" and moved the depth into the walkthroughs, which left the template asking an owner to write an index to a section that did not exist yet. An index is written last. The owner now writes the sequences they were going to write unprompted anyway, then reads them back and names what each one delivers. **The cross-reference reverses with it.** A walkthrough heading can no longer say *serves `F1, F3`* about numbers that are not written when it is written, so each `F#` item carries **"delivered by"** instead, naming the walkthroughs that deliver it — or `—` where the item is genuinely a property rather than a sequence, which is the one legitimate empty. That is worth more than the tidiness: **coverage stops being a checklist item and becomes a property of the writing order.** An item with nothing to name has a visibly empty line, and a walkthrough delivering nothing shows up the moment the list is finished. §12 gains one readiness check, in both directions. **Two fields deleted.** *How important* (`must have / would like / one day`) had **no consumer anywhere in the standard** — no handover row, unread by `Logic.md`, and build order comes from the dependencies between parts via `Railroad.md` §2, never from an owner's ranking — while being the only place in the document where an ask could be lodged in the conditional. A specification records what **must be**; a thing wanted at half weight is a §6.2 entry, and the callout now says exactly that. *How much / how often* was **relocated rather than deleted**, because it is the one field of the four with a live consumer: Appendix C, where a missing figure is a question to ask and never a number to invent. Frequency is a property of the flow, not metadata about a feature, so the figure is now written into the step that carries it, and the handover row points at the steps instead of at a field. The prompt survives; the meta-field does not. **Two fields renamed:** *What I want* → **Definition**, and *How I will know it worked* → **Expected result**, with the observability test demoted to the hint underneath — the new label reads as a noun where the old question forced a check, so the check had to be written down rather than carried by the wording. The judged answer, *"I would have to look at the results and judge them"*, is still legitimate and still seeds P11. **Deliberately NOT changed:** `F#` numbering and its never-renumber rule, the required §3 tree, §6's three lists, §7's questions, §11, the freeze, and the register the document is written in — no filenames, functions or engines outside §6.3.
* **Mk II Mod 1 A2** — *the verification rule restated positively; the prohibitions demoted to consequences. Nothing becomes permitted or forbidden that was not already.* The standard had a real rule — one altitude, Architect-authored criteria, two files per chapter — and stated it as a taboo on a kind of file: *"the Builder never creates a test file, fixture, harness, or testing dependency… a hard MUST-NOT."* Seven places repeated some version of that list as though each were its own law, and the collision was unreconcilable on the page: R3.3 mandates an executable file that boots the system, reaches through observation seams, provokes races, prints `PASS`/`FAIL`, and may legitimately pull **a testing dependency** (through Step 0.0's freeze) and **a runner config** (§0.5) — while §0 called exactly those objects a hard MUST-NOT. The rules never actually conflicted, because they are scoped by altitude and authorship; only the vocabulary did. But a Builder reading both had to derive the reconciliation alone, every session, and **the cost of that is on record in this very history**: Mod 0 documents a project growing per-step evidence files because rail 6's observation need had nowhere to go, which is a taboo being obeyed in letter and routed around in fact. **What changed:** `Railroad` §0's verification block is now the single statement of the rule as three numbered clauses — altitude, authorship, cap — followed by what they rule out and by a paragraph naming the vocabulary trap outright (*judge an artifact by altitude and authorship, never by its filename or its imports*). Rail 2 keeps authorship as clause 2 and stops re-arguing the altitude. `§0.3`, `R3.1`, the `§0.5` tree note, both step templates and the scaffold step now cite the clause each prohibition descends from instead of restating the list. R3.3's constraint heading stopped claiming to keep the artifact "from becoming a test suite" — it *is* one by shape, which was never the question. This README's testing section leads with the three clauses and keeps the reasoning underneath. **One item was not vocabulary and is the reason this entry is load-bearing.** Global invariant 3 — an invariant asserted over *every step* — still read **"the Builder never creates a verification artifact of any kind"**, a Mk I sentence that Mod 0 outgrew the moment it moved evidence authorship to the Builder. It therefore flatly forbade the file that R3.2/R3.3 and the chapter's last step all require, and it had survived the Mod 1 A1 consistency pass. It now states clause 2 as the invariant and names the single file the Builder does author, with the superseded wording noted so the next reader does not book it as a regression. **Deliberately NOT changed:** the altitude itself, the two-file cap, who authors criteria, the 1–4 `V#` budget, and A3's Pattern gating — the last of those is real debt (A3 bundles three jobs whose justifications are not all class-dependent, so a many-chapter Pattern I project is denied the tripwire that exists to bound its manual regression), and it is a Mod, not this.
* **Mk II Mod 1 A1** — *consistency pass over the two Mk II bumps; no new rule.* A full audit of the toolkit against itself, run under the **Consistency** criterion, found that Mod 0 and Mod 1 had landed in `README` and `Railroad` but only lightly in `Logic Template.md`, and that both bumps had left their own counts behind. Nothing here is a decision; every item aligns a file with something the standard had already settled, which is what makes it an A rather than a Mod. **Two were load-bearing.** The pin instruction still read `Built against STC Mk II Mod 0 A0` while this file's header declared Mod 1 — so every project copying that line would have pinned itself one Mod behind and inherited none of Mod 1's criteria rules, silently. And `Logic` §2.1's tree had `acceptance/` nested **inside** `directorium_temporarium/`, the bin that is emptied at every chapter gate — which would have deleted the very checklists the re-walk depends on, and did not know about `evidence_chapter_<n>` at all. `acceptance/` is now a root sibling carrying both artifacts and their owning steps, matching `Railroad` §0.5; the bin's comment now states outright that no verification artifact may live in it. That matters more than a path fix, because global invariant 7 and `Railroad` §0.5 both name **`Logic` §2.1 as the authoritative architecture** — the file everything defers to was the one that was wrong. `manuals/` also stopped claiming to hold the Logic and Railroad documents, which live in the design workspace, never in the built repository. **Stale counts:** `Logic` §0 announced "three rules" over four (Mod 1 added the `V#`-invalidation rule), and `Railroad` R8's class-gating rationale still said "eight defect classes" after Mod 1 added classes 9 and 10. **The audit criteria disagreed across files:** `Railroad` R8.1 asks the Architect to declare one of "the four criteria (`Logic.md` §0)", but the two tables listed different fours — `Logic` was missing **Mechanical-ness**, the rail 6 class a consistency pass reads straight past, and R8.1 was missing the owner's annotation pass. `Logic` §0 now names five and says which four are R8.1's; R8.1 says the fifth is not the Architect's to run. **Dangling references, all corrected against their real targets:** three places cited "§2.1 rule 4" for *deferred means absent*, which is rule **5** (rule 4 is the over-engineering list) — `Logic` §13, `Railroad` §0.2 and global invariant 9; `Logic` §4.1 pointed at §3.4 for the cancellation contract, which is §4.4; P11's evaluation contract was cited as §6 in two places against the profile row's correct §3.6; `Railroad` sent a reader to "§11 below" for the global invariants, which are not numbered §11 in that file, and to `R6.4`, which does not exist. The scaffold step listed `V4` under `Verify` and closed on "V1-V3". The floor list omitted **§3** entirely — the application logic, and the point of the document — along with §3.5, where the owner's own prohibitions land. One rendering fix: two profile columns in the §0.5 tree had collided into `(§8.5)└─ backends/`. **Deliberately NOT changed:** the stale `P1`/`P5`/`P6` labels that predate the a/b split, the surviving domain leakage from the extraction source, and the duplicate-tree arrangement itself — all three are real debt, none is a Mk II regression, and each is its own bump.
* **Mk II Mod 1 A0** — *criteria hygiene: three ways a green `V#` can be worthless, and one clause each.* Mod 0 fixed *how many* verification artifacts a project writes; this fixes *whether the criteria inside them can be trusted*, which is the prior question and the one the same audit answered badly. Three failures, all observed on the same build, all of which read green: **(1) A transition set proven only along the path it describes.** A circuit breaker's criterion walked `CLOSED → OPEN → HALF_OPEN → CLOSED` on exactly the pinned counts and passed for weeks, while a successful probe arriving *during* the cooldown closed the breaker outright — skipping the timeout and the entire half-open probation, an edge the decision never granted. The criterion never met it, because it advanced the clock past the cooldown before its first success, so the illegal state was never the state under test. **Rail 6 gains a second corollary:** where a `D#` pins a state machine or an ordering, one `V#` asserts a transition the law does **not** have. It costs a clause inside an existing criterion, not a fifth one. **(2) A criterion outdated by the very amendment that moved its law.** An A-bump made "a refusal is answered once" the rule; the criterion beneath it still demanded the *silent* drop it had replaced — so it asserted the previous law in its own words and would pass only on code that was now wrong. A missing criterion is silent; this one certifies. **`Logic Template` §0 gains amendment rule (4):** a bump lists the `V#` it **invalidates**, by number, and a bump that changed a rule while invalidating nothing is a claim to re-check rather than a clean result. **(3) A criterion satisfied by fabricating its own precondition.** One demanded a shielded side effect and its delivery-log rows at a step with no tool layer, no such log and no shielded section anywhere; greening it required a private registry present in no appendix plus an injected task, so scaffolding was proving the criterion rather than the product. Rail 7's corollary already forbade this as an *Architect's* obligation, which is the wrong end — the person who can see it is the one being asked to do it. **Rail 4's `ACCEPTANCE-DEFECT` sub-type and the step-close checklist both gain the Builder-side trigger:** *did observing any `V#` require a structure absent from Appendix B?* If yes, the step does not close. It needs asking out loud because supplying the missing structure feels like implementation work rather than like the contract violation it is. **Also:** `R8.2` gains classes **9** and **10** for the first two, since both are paper-visible once you know the tell — read every `V#` citing a `§` the bump touched, and count the transitions a `D#` names against the ones any `V#` actually enters. **Deliberately NOT changed:** the 1–4 `V#` budget per step, the artifact cap, the altitude, and who authors criteria — all three fixes are clauses inside existing criteria, not new ones.
* **Mk II Mod 0 A0** — *the verification artifact set, corrected against a measured build.* The trigger was a Pattern III project reporting that per-step evidence files were burning tokens out of proportion to what they proved. Reading the evidence rather than the intuition confirmed it and went further: **1929 lines of evidence against 1414 lines of the product code they observed**, the same scaffolding written six times, and — the finding that actually decided this bump — **not one of the five real defects found in that project's audit was caught by those files.** All five were found by reading code against the law, while every evidence file passed. The scripts confirmed the fixes afterwards; they did not find the defects. Two conclusions follow, and they point in opposite directions, which is why Mk I got this wrong in both places at once. **First:** evidence artifacts catch *regressions*, audits catch *defects*, and pricing the first as if it did the second is what made per-step files look worth their cost. **Second:** the need underneath them was real. Rail 6 requires a pinned mechanism to be exact, and a human walk structurally cannot observe one — a person driving the system by hand cannot see a bucket depth, a refill interval, an ordered effect trace, or a gate that was never consulted. Mk I had **nowhere to put that observation**, so the project invented per-step files to hold it; the cap was breached by a legitimate need rather than by indiscipline, and re-forbidding it would only have made the need reappear elsewhere. **What changed:** the per-chapter smoke script (A3) and the Pattern III probe (A4) **merge into one `evidence_chapter_<n>` artifact** carrying three jobs — tripwire, mechanism observation, provocation — written by the **Builder** at the chapter's **last** step, against code that has stopped moving, with the constraint that **every observation traces to a `V#` the Architect already wrote** (which is what keeps rail 2 intact under Builder authorship). A4's number is retired and never reused, because `PATTERN-EXCESS` counts against it. The artifact cap becomes **two per chapter**. `R3.1` gains the rule naming what actually protects the window between a chapter's first and last step — **the re-cut**, a prose pass costing no artifact, which is where every `ACCEPTANCE-DEFECT` this standard has caught was caught. **`R3.2` gains an owning step for `CHAPTER_<n>_ACCEPTANCE.md`:** Mk I mandated that file and named no step that creates it, which is the *structure with no owning step* defect that **rail 7 exists to forbid** — the standard was committing its own violation, and in the project that surfaced it the checklist was never written, leaving a chapter gate to be signed off a developer trace. It is now created by the chapter's first step. **Also folded in, and named rather than smuggled:** the throwaway bin is `directorium_temporarium/` everywhere, retiring the `_scratch/` spelling that Mk I used in the README and `Railroad §0.3` while `Logic Template §2.4` already used the deliberate name. **Deliberately NOT changed:** the rails, the step template, the stub protocol, the reading order, the freeze, the four documents, the profile and the Patterns — and the verification *altitude*, which stays at the chapter. *(Housekeeping note for a later editor: the Mk I list below declares itself "oldest first" and is not — entries after Mod 2 run newest-first. It is left exactly as written, because a revision history that gets retrofitted stops being one.)*

**Mk I** *(preserved in `ARCH/`; not superseded retroactively)*

* **Mk I Mod 1 A0** — the baseline standard: four documents, the six rails, the profile, scaffold-first build, chapter-altitude verification with the artifact cap.
* **Mk I Mod 2 A0** — *extraction pass against a mature source system.* Added **rail 7** (provisioned & reachable surface — a frozen name also needs a source that fills it and a path the caller can reach it by), **global invariants 11–12**, **Appendix R8** (the Architect-side audit protocol, with its four criteria and eight recurring defect classes), `Logic` **§2.7** (framework adoption boundary), **§8.4** (admission: gates ahead of what they protect, no externally-chosen work before admission), **§9.4** (the alert channel as a named mechanism, plus the observability split), the amendment/claim discipline in §0, the provisioning triad in Appendix B/C, and the independence invariant for recovery in §9.1. Fixed the standard's own leakage: `requirements Template` pointed at the source system's section numbers rather than STC's.
* **Mk I Mod 7 A0** — *specification §6 and §7 rebuilt as writing surfaces rather than explanations.* The owner reported not being able to feel **how these two sections were meant to be used**, which on inspection was a fair description of the page: §6 offered three placeholder blobs under fourteen lines of taxonomy, a resolver table and three worked paragraphs, and §7 was a bullet list of questions whose options were separated by interpuncts with no indication of whether you pick one, delete the rest, or write a sentence. The sections that already worked — §3's tree, §4's `F#` block, §5's numbered walkthrough — all share one property those two lacked: **they show the shape of a filled entry.** Both are now built that way. §6 is split into numbered subsections **§6.1 must never**, **§6.2 not in this version** and **§6.3 imposed anyway**, each a short list of flat bullets carrying three example lines in the tone and length to imitate, with the taxonomy compressed from a table and three paragraphs into a single "ask who could break it" note. §6.3 additionally requires a **reason on every line**, since a choice with no reason cannot be costed or argued with, which was the whole justification for keeping those constraints separately in the first place. §7 gains an explicit answer slot per question with the option list demoted to an italic hint underneath, plus one worked answer at the top so the expected register — a plain sentence, no mechanism — is visible before the owner starts. The numbered subsections also retire the awkward prose references: what used to be *"§6's 'imposed anyway' list"* is now just **§6.3**, throughout the template, the readiness list and `Logic Template.md`'s handover map. Two readiness checks added: every §7 question answered in the owner's own words, and every §6.3 line carrying its reason.
* **Mk I Mod 6 A0** — *specification §11: the known direction, separated from deferred work.* The specification could record a **no** ("not in this version", §6) but had nowhere for a **yes, eventually** — a direction the owner already believes in and is deliberately not specifying. Owners write that anyway, in the margins of a feature or as an aside in a walkthrough, where it reads as a request and gets built. New §11 gives it a home between the clarifications and the readiness list, deliberately *after* everything that is actually being asked for. Two rules make it safe rather than dangerous, because a "coming later" list is the shortest route to speculative generality there is: it says **what** is coming and never **how**, and it **may break a tie, never buy a structure** — where two designs are otherwise equal the one that does not foreclose a listed direction wins, but nothing on the list may be cited as the reason an abstraction, a flag, an extension point or a spare layer exists today. It is therefore not deferred work and does not land in `Logic.md` §13; it is recorded in §0 as non-normative known direction, and a line naming a specific engine is a choice in disguise that belongs in §6's "imposed anyway" list. The readiness list moves to §12 and gains one check: §11 describes no mechanism.
* **Mk I Mod 5 A0** — *the specification's two constraint sections merged; twelve sections become eleven.* A5 sharpened the §6/§7 boundary with a test and a table, and the owner still read them as one thing — which is the answer: if a distinction needs a table to survive contact with the person writing the document, it is not a section boundary, it is a **sorting rule inside one section**. Old §7 became the third labelled list of §6, now titled *"what it must not do, what we are not building, and what I am imposing anyway"*, with three halves: **must never** (a rule the running program could break), **not in this version** (scope, which nothing breaks), and **imposed anyway** (a rule only a wrong build could break, and the sole place in the document permitted to name a technology, a machine or a place). The "who could break the rule" table stays, but now resolves *within* §6 and against §7's terrain, which is where the confusion actually lived. Everything below renumbered: old §8→**§7** (how it lives in the world), §9→**§8**, §10→**§9**, §11→**§10**, §12→**§11** (the readiness list). The handover map in `Logic Template.md` §0 was rewritten against the new numbers, and the terrain-versus-choice contrast now names the "imposed anyway" list rather than a section. **Reading the entries below:** revisions before this one refer to the specification's *old* numbering, where the imposed constraints were §7 and the world questions were §8; they are left as written, because a revision history that gets retrofitted stops being one.
* **Mk I Mod 4 A5** — *the §6 / §7 / §8 boundary made mechanical.* Owners could not tell the specification's three constraint sections apart, and the template was at fault twice over: it carried an explicit §7-versus-§8 contrast but **no §6-versus-§7 one at all**, and §7's own example list contained a bare prohibition (*"it must not need the internet"*), which is grammatically indistinguishable from a §6 rule. All three sections say *"it must / it must not"*, so grammar was never going to separate them. The axis is now stated once, as a **single test — who could break this rule**: the running program (§6, a rule about what the thing *does*, checked every run, never traded away), only a wrong build of something the owner *chose* (§7, a rule about what it is *made of* and where it lives, checked once, challengeable with a cost attached), or only a wrong build of something **nobody chose** (§8, terrain, which has no price and may only be designed around). A three-row resolver table plus worked examples lives in §7 — the middle case, and now the only place the distinction is explained, replacing two prose paragraphs that said overlapping things in different words. A second, purely mechanical tell reinforces it: **§6 may not name a technology, a machine or a place, and §7 is the only section that may** — so a line needing one of those words to make sense is a §7 line by inspection. The colliding example was reassigned: *"it must not need the internet"* is now shown splitting both ways depending on why, which is the reason worth recording. The assistant's mandate gains one clause: it may move a misfiled line to the section it belongs in.
* **Mk I Mod 4 A4** — *specification §3 tree is required, as the textual form.* The structure sketch is not an optional drawing aid: it stays in the template because it is how the parts are introduced in the document — a nested tree, one-line responsibility on each line — so later walkthroughs and `Logic.md` §2.1 have names to hang from. The tree is still not a decision; `Logic.md` may replace it. What it is not allowed to be is absent.
* **Mk I Mod 4 A3** — *specification template in two languages.* The owner-facing specification now exists as an English file (`Specification Template.md`) and a Polish twin (`Specification Template PL.md`) with identical section numbers, identical `F#` identifiers, and identical freeze rules. The owner picks one language and writes the content in it; the project artifact is still named `Specification.md` so `Logic.md` and `Railroad.md` do not have to know which language was chosen. Nothing else in the standard is bilingual: law, railroad, and the rest of the toolkit stay English.
* **Mk I Mod 4 A2** — *environment split into choice and terrain (specification §7 / §8).* The specification now captures where the thing will actually run and where it will be built, which the previous revision had nowhere to put — but as **two different kinds of fact rather than one**: an environment the owner *chooses* ("it has to run on my machine") stays in §7, where it can be costed and revisited; an environment that simply *is* ("the machine in the warehouse reboots nightly", "no admin rights on that laptop") belongs in §8 as terrain, which may only be designed around. The handover map keeps them apart in `Logic` §0 TARGET ENVIRONMENT for the same reason, so a later reader can tell which assumptions are negotiable. A toolchain remains in neither: naming a framework or an engine is a decision, and it is made in `Logic.md` against everything the specification says, not picked before anyone knew what the thing had to do. The "where will it be built" answer also seeds the Dev Log's machine-notes table on day one, which is the case that motivated the Dev Log in the first place.
* **Mk I Mod 4 A1** — *specification reshaped against a real first-draft.* Reviewing an actual owner-written first attempt showed the template was universal in intent and **shaped like an office-productivity tool** in its prompts: it asked who uses it and when, it assumed a current manual process worth describing, and it gave the owner no room for the thing they actually produce unprompted — a component-by-component walkthrough of how they imagine it working. Reshaped: §2 is now **the premise and direction**, and it is the **reference point** every item is checked against, since a divergence between premise and feature list is the earliest and cheapest visible sign of a project going wrong; the **structure sketch** is a first-class optional section, explicitly non-binding, because drawing the parts is how some owners design; `F#` items became a short numbered index, with the depth moved to **§5, the walkthroughs**, grouped however the owner thinks and as long as each needs, each naming the `F#` it serves and what should happen when it goes wrong; the two kinds of "no" (a standing prohibition versus a scope exclusion) merged into one §6 with labelled halves, since owners read them as one thing while they land in different places; **§8 collects the five facts only the owner knows** — what starts it, who receives the result, whether it should run by itself, what information it touches, and what it should do when something fails — as plain questions where *"I do not know"* is a complete answer; the "how it works today" section became explicitly deletable for greenfield projects. The handover map from those answers onto profile rows was added to `Logic Template.md` §0, deliberately **not** to the specification: the owner answers plain questions and never learns what a profile row is.
* **Mk I Mod 4 A0** — *multi-reviewer correction pass, plus two new artifacts.* Three independent reviews of Mod 3 (each declaring its own criterion, per §0 of the logic template) produced disjoint findings, and the sharpest was that **Mod 3 was implemented faster than it was derived**: several of its class rules were set by taste rather than by an extraction card, which is rail 6's own defect class applied one storey up. Corrections: **P1 split into P1a/P1b** (a single external dependency now registers — previously a program calling one API answered NO to every row and landed on the floor, inheriting no timeouts, no secrets handling and no admission section); **P5 split into P5a/P5b** and **P6 into P6a/P6b**, because each conflated two different obligations and pushed attended, single-run concurrency into the unattended class; **P11 reworded structurally** — it now asks whether correctness can be asserted exactly, rather than naming a technology, and no longer raises the class; **P12 no longer raises the class** either. **Removed as unsourced:** per-class minimum chapter counts (a volume metric, which the derivation protocol forbids) and the class-gating of the LONG CHAPTER protocol. **Added:** the per-chapter **probe** (apparatus A4) for Pattern III, because a happy-path smoke script cannot see a race, a lost write, a duplicated effect or a starved loop, and the standard was previously pretending the human walk covered them. **Consolidated:** the class rule now lives only in `Logic` §0.1, the apparatus only in `Railroad` §0 as a **numbered catalogue** (so `PATTERN-EXCESS` counts something enumerable), and this README explains rather than restates; the per-class section lists were deleted as a second copy of what the profile already says, and the four over-engineering prohibitions moved to `Logic` §2.1. **Gates tightened:** a `PATTERN-WAIVER` must name its compensating control, and the `STC-GAP` **register** lands here — the first and only piece of state STC keeps across projects. **Two new artifacts, neither of them law:** `Specification Template.md` (owner-authored, plain-language, frozen to `ARCH/` when `Logic.md` opens, with `F#` numbers that survive into §3 so drift is answerable) and `DevLog Template.md` (the Builder's append-only journal, living with the code for continuity across machines, compacted at every chapter gate, and forbidden from ever holding a decision). **Standing caveat:** the three classes were derived from cards written *after* the solutions had already been transferred, which the protocol names as its own anti-goal — the boundaries are therefore **provisional** and are to be re-derived on the next extraction run from cards written before transfer.
* **Mk I Mod 3 A0** — *Patterns.* Complexity classes derived from the same extraction by asking, per mechanism, the minimum profile at which it starts paying for itself; two clusters emerged above the floor, so there are three classes. Adds the classification rule and its six properties, the per-class mandatory/optional/**forbidden** lists, the immovable/movable boundary, the Mod-not-Mark resolution for class crossings, and the four gate devices (`PATTERN-WAIVER`, `PATTERN-EXCESS` with its limit of three, `STC-GAP`, version pinning). Adds profile row **P12** (an adopted framework), which the extraction surfaced as a missing row: §2.7 had no profile gate, which is the same defect the standard books as *specified structure with no configured source*. **Known open items:** whether P4-driven obligations form a fourth class (no mechanism extracted so far requires them); whether P2 belongs in Pattern II or III; and whether the floor is in fact the most populated class — all three need a population of real projects to settle.

---

## The core methodology (`Railroad Template.md` §0) — keep this almost verbatim

`Railroad Template.md` §0 is pure process, with zero domain content — a general contract for turning a frozen spec into working code via small, non-improvised steps under a fixed verification budget. Highlights, so you know what you're buying into before you adopt it:

1. **Seven rails** — frozen public surface, frozen *acceptance criteria*, a MUST-NOT list per step, HALT-on-gap (`CONTRACT-GAP`) instead of improvising, small steps, frozen parameters (a bare adjective like "reasonable timeout" is a gap, not a spec — pin the number or the formula), and a provisioned surface (a frozen name also needs a **source** that fills it and a **path** the caller can reach it by — otherwise it is specified and unbuildable).
2. **Stub protocol** — the entire target tree is scaffolded first, every file a stub with a typed canned return; each phase *fills* stubs, never adds or moves files.
3. **`CONTRACT-GAP`** — the single escape hatch when the spec is wrong, missing, or ambiguous. The builder halts and reports it precisely instead of guessing; the architect resolves it with a `Logic.md` amendment (an A-bump, usually).
4. **Verification at chapter altitude, executed by a human** — see below. One altitude, Architect-authored criteria, two files per chapter; a step closes on a *printed checklist*.

### Where testing lives (and why it is not where you expect)

**The rule is three clauses, and it is stated positively on purpose** — `Railroad Template.md` §0 owns it: executable observation exists at exactly **one altitude** (the chapter gate); **what** is proven is always the Architect's, written as a `V#` before any code exists; and the whole budget is **two files per chapter**. Everything that sounds like a prohibition in this standard — no per-step test file, no fixtures module, no mock library, no `tests/` tree — is a *consequence* of those three, and none of them is a rule you could break independently.

That ordering matters, and Mk I got it backwards. It led with the prohibition, which reads as a taboo on a *kind of file*; and a taboo on a kind of file is obeyed in letter and routed around in fact. The proof is in this standard's own history: rail 6 requires pinned mechanisms to be exact, a human walk cannot observe one, Mk I had nowhere to put that observation, and the project riding it grew per-step evidence files to hold it. The need was legitimate and the rule had made it unspeakable. **So judge any proposed artifact by altitude and authorship, never by its filename or its imports** — the chapter evidence artifact is an executable file that boots the system, provokes races, prints `PASS`/`FAIL` and may pull a testing dependency, and it is permitted for exactly two reasons: where it sits, and whose criteria it serves.

A build step's proof of correctness is therefore a **printed ✓/✗ checklist**: each frozen decision (`D#`) and each observable criterion (`V#`) with a one-line proof — a value seen, a log line, an exit code. Nothing is written to disk for verification purposes.

Real verification happens at **chapter** boundaries. A chapter = one phase = one row of the milestone ladder = **a user-testable MVP increment**. Its gate is a short **Acceptance Checklist** written in the owner's language (*do this → expect to see that*), walked **by a human**, together with a re-walk of every earlier chapter's checklist.

Beside it sits the chapter's **one evidence artifact** (`Railroad Template.md` R3.3), written by the Builder at the chapter's last step against code that has stopped moving. It exists because a human walk, by construction, cannot see everything the rails pin: a person driving the system by hand cannot observe a bucket depth, a refill interval, an ordered effect trace, or the fact that a gate was *never consulted* — yet **rail 6 requires exactly those to be exact**. That gap is real, and Mk I had nowhere to put it, so projects grew per-step evidence files to fill it. Mk II names the need and gives it one artifact with three jobs: re-walk tripwire, mechanism observation, and provocation of the invisible properties (a race, a lost write, a duplicated effect after restart, a starved loop) that Mk I handled with a separate Pattern III probe.

**Verification artifacts are capped at two per chapter — the checklist and the evidence artifact.** If a project ever holds more verification files than twice its chapter count, the standard has been violated.

**Written at the end of the chapter, never step by step**, and that is a measured position rather than a taste. A Pattern III project built one evidence file per step and ended with **1929 lines of evidence against 1414 lines of the product code it observed**, the same scaffolding written six times. Running them was never the cost — a full re-walk of 41 criteria prints six lines. **Re-cutting them was:** one amendment moved a gate mid-chapter and four of six files had to be rewritten. What protects the window between a chapter's first and last step is not an artifact at all; it is the **re-cut** (R3.1) — the Architect reading each step's `V#` against that step's frozen surface before any code exists.

**When a chapter genuinely cannot be small**, the railroad has one narrow siding: the **LONG CHAPTER protocol** (`Railroad Template.md` §0.3). It is declared with a written reason — *"no intermediate state of this is observable"*, never *"this is a lot of work"* — and its checklist is segmented into at most five checkpoints, each walked as it is reached, closing with one end-to-end pass. Later chapters re-walk only that final pass, so the regression walk stays bounded.

Three reasons this is the design, not a shortcut:

- **Token economy.** A per-step suite is written, run, re-run for every later step (quadratically), and repaired when it breaks. For an AI Builder that cost dominates the actual code generation — and it buys a fourth restatement of a constraint rails 1, 3, 4 and 6 already enforce.
- **Legibility.** A check the project owner cannot read is not verification; it is a second unreviewed artifact that has to be trusted on faith.
- **Sequencing.** Tests before a working proof of concept protect an interface that has not been demonstrated yet. STC's answer is: demonstrate the chapter, *then* decide what deserves automation.

The cost is named openly in `Railroad Template.md` §0.3: between two chapter gates, a silent regression can go undetected. That window is bounded by the chapter, and it is the price paid for the budget. **Adopting a real automated suite later is a legitimate decision** — the owner's, taken once there is something worth protecting, and planned as its own chapter. It is never something a coding agent starts on its own.

If you only take one thing from STC without reading further, take this: **freeze the interface before generating any code**, and **treat every unpinned parameter as a defect**, not a detail to improvise later.

---

## Using this template for a new project

0. **The owner writes `Specification.md` first**, from `Specification Template.md` **or** `Specification Template PL.md` (pick one language and stay in it), alone and in plain language — what the thing should do, how they will know each feature worked, what it must never do, what it is deliberately not doing. Then they talk it through with the agent, in rounds, until *they* are satisfied. The agent asks, contradicts and points at gaps; it does not design here. **Nothing below starts until the readiness list at the end of that template passes.** Skipping this step does not save time: it moves the same conversation into `Logic.md`, where it gets held in technical vocabulary the owner never agreed to. The filled file is always named `Specification.md`, whichever template language was used.
1. Copy `Logic Template.md` → `<new-project>/Logic Mk I Mod 1 A0.md`, `Railroad Template.md` → `<new-project>/Railroad.md`, `dependency Template.md` → `<new-project>/dependency.md`, `requirements Template.<ext>` → `<new-project>/requirements.<ext>` (pick the extension for your stack), and `DevLog Template.md` → the built project's `manuals/DevLog.md`. On the same day, freeze `Specification.md` and move it to `ARCH/` — the features keep their `F#` numbers so `Logic.md` §3 can cite them.
2. **Fill in the Project Profile (`Logic.md` §0.1) before writing anything else, compute the Pattern from it, then delete every section its NOs and its class point at** — in `Logic.md` and in `Railroad.md`'s file tree and appendices alike. Do this *first*: it is far easier to delete twelve rows' worth of structure from an empty skeleton than to notice, six chapters in, that you have been maintaining a persistence layer for a program that persists nothing.
3. **Write §2 (architecture) and §3 (application logic) before anything else in the document, and give §3 the most time.** §2.1 fixes where every responsibility lives; §3.3 says what each module does, in what order, and *why*. The later sections and the appendices are derived from these two — written in the other order, they become contracts nobody can justify.
4. Work through what remains of `Logic.md` top to bottom, replacing every `<...>` placeholder and `[STC: ...]` instruction comment with real content. Don't delete a section your profile says applies just because filling it out is effortful; that effort is exactly what the freeze buys later.
5. Freeze Appendix B (Contract / Ownership Map) and Appendix C (Config Keys) — these two are what let a coding agent (or a new teammate) build without guessing. Nothing in the prose above them may name an identifier absent from these appendices.
6. Only then write `Railroad.md`: walk the dependency graph from `Logic.md` Appendix A, decide build order (definition order ≠ build order — see Railroad Template §1), draw the chapter ladder so that **each chapter is a capability the owner can exercise by hand**, and author Phase 0 in full `Step N.M` detail before writing any code.
7. Every `Logic.md` revision from here on is a version bump with a Revision History entry — never a silent edit. Turning a profile NO into a YES is a **Mod** bump.

**Two sanity checks before you start generating code.** First: could someone tell what this program *does* from your `Logic.md` section list alone, or only that it was built from a template? Headings surviving with unfilled placeholders under them are the visible symptom of a profile nobody answered. Second: is §3 the longest part of the document? If the appendices outweigh the reasoning, you have written a specification of a shape rather than of a program — and the most expensive review comments will all arrive after the code exists.

---

## AI/LLM projects — the sanctioned skill base

**Clause (applies only when the project involves AI/LLM work).** For projects that build or integrate AI agents, a curated **skill base** is available and the coding agent **may** consult it. This is the one deliberately environment-specific part of STC: a project started outside this workstation simply deletes the clause. Everything else in the standard stands on its own.

It is no longer a folder you read — it is a **Claude Code skill plus two queryable graphs**, so the normal way in is to invoke the skill, not to open files:

| Entry point | What it is |
|---|---|
| **`skill-base` skill** | user-scoped (`~/.claude/skills/skill-base/SKILL.md`), reachable from **any** project. The intended front door: it carries the registry and the query recipes. |
| **Vault root** | `G:\Mój dysk\DEV DEPARTMENT GD\AI-LLM\ObsiClaude` — the base itself. Corpora live under `REPOS/`; `CORPORA.md` at the root is the authoritative registry. |
| **`aid4-knowledge`, `graphify`** | vault-local skills — the detailed guides to, respectively, the curated theory graph and the code graphs. Loaded only when working inside the vault. |

### Step 0 — availability check, once at the start

**Do this before planning to use it, not after a path fails mid-task.** Probe in this order:

1. **Is the `skill-base` skill available in this session?** If yes, invoke it and stop probing — it self-describes the registry, the query surfaces, and the recipes. Nothing else to configure.
2. **Else, does the vault root resolve?** Skill absent but vault present means *this machine was never bootstrapped* — the vault syncs (Google Drive), the user profile does not, so the skill and the cross-corpus graph are simply missing here. One idempotent command restores them, and it costs seconds rather than minutes because every expensive per-corpus graph is already inside the vault:

   ```powershell
   powershell "G:\Mój dysk\DEV DEPARTMENT GD\AI-LLM\ObsiClaude\bootstrap\install-skill-base.ps1"
   ```

   Propose it; do not run it unasked.
3. **Else — ask the user. Do not conclude it is gone.** A failed path check proves almost nothing here: the path has spaces and a non-ASCII character, the drive letter differs per machine, and Google Drive may not be mounted yet. Ask plainly, e.g.:

   > *"Is the curated AI/LLM skill base available on this machine? I expected it at `G:\…\AI-LLM\ObsiClaude` but that path doesn't resolve — if you have it, where does it live here? Not a blocker; I'll build without it either way."*

**Record the answer once per project** (a line in the project's notes or `Logic.md` §0 is enough) so it is not re-litigated every session. It does **not** go in `dependency.md` or `requirements.<ext>` — those describe what the *built system* needs, and the skill base is an authoring accelerator that the delivered project must never depend on. **A project stays fully buildable from `Logic.md` alone.**

### What is in it

`CORPORA.md` is authoritative — a corpus earns a place by being *worth imitating*, and each row states why. Snapshot:

| id | Kind | Contents |
|---|---|---|
| `aid4-knowledge` | theory (**WHY**) | 25 Polish course notes (`s01e01`–`s05e05`), ~880 KB, + 280 diagrams. Agent design, RAG, MCP, memory, evals. |
| `aid4-code` | reference code (**HOW**) | 53 runnable TypeScript examples + shared MCP servers + a production Drizzle/SQLite multi-tenant agent schema. |
| `wonderlands` | production system (**HOW**) | A real Svelte + Hono + Drizzle monorepo: Result-type error handling, tenant scoping, MCP client, sandbox runtime. |
| *(unregistered)* | third-party | `REPOS/AID4-CODE/Mentioned repos/` — 31 vendored repos, 1.3 GB. Deliberately **not** graphed; read on demand. |

Topics covered (the theory graph's controlled vocabulary is 49 concept ids): prompting, structured output, function calling, tool design, agent design, workflow/orchestration, multi-agent, MCP, RAG (chunking / embeddings / vector-db / hybrid-search), long-term memory, knowledge graphs, context management + compaction, multi-thread context, evaluation, observability, prompt optimization, human-in-the-loop, generative UI, security/privacy, model limits, production/deployment, agent DB schema, sandboxing, coding agents, browser automation, and more.

### Two layers — pick the one that answers your question

| Question | Layer | Surface |
|---|---|---|
| *Why* is it done this way? What principle or pitfall applies? | curated theory (hand-mined) | `REPOS/AID4-SKILL/knowledge-graph/` |
| Is there a **diagram** of X? | curated visual | `knowledge-graph/kg-img/catalog.jsonl` |
| Show me a **working implementation** of X | code graph (AST-generated) | `~/.graphify/global-graph.json` |
| What calls what? What breaks if I change X? | code graph | same, or one corpus's `graphify-out/graph.json` |

**Ground the design in the WHY layer, then find the implementation via the HOW layer.** They are not interchangeable: theory has intent and pitfalls but no current call graph; the code graph has exact structure but no reasoning.

### How to use it — graph first, files second

The base is **large enough to destroy a context window if read naively.** Both layers exist precisely so that never happens. The workflow is non-negotiable: *query the index, then open only what it names.*

**A. Code graph (HOW).** Local graph traversal — **zero LLM cost**, no model call:

```bash
graphify query   "how are tool executions persisted" --graph ~/.graphify/global-graph.json
graphify explain "TenantScope"                       --graph ~/.graphify/global-graph.json
graphify path    "<A>" "<B>"                         --graph ~/.graphify/global-graph.json
```

- Each hit carries `symbol`, `source_file:line`, the named subsystem it belongs to, and a confidence tag — `EXTRACTED` (explicit in source) or `INFERRED` (resolved by the tool). Weigh `INFERRED` accordingly.
- Node ids are namespaced per corpus (`aid4-code::…`, `wonderlands::…`). When a name matches in several corpora the tool lists them side by side — **that comparison is often itself the answer** (how a teaching example did it vs. how the production system did it).
- Narrow a sprawling result with `--budget`; drop from the global graph to one corpus's `graphify-out/graph.json` when the question is scoped to a single system. For architecture-level review (hub nodes, import cycles, communities), read that corpus's `graphify-out/GRAPH_REPORT.md` instead of querying.

**B. Curated theory (WHY).** A greppable index — never bulk-load the notes:

1. **Map the task to 1–3 concept ids** from the vocabulary in `knowledge-graph/graph.json` → `concepts`.
2. **Grep `graph.json`** for each concept id → matching `lessons[].file` (theory), `code[].dir` (reference implementation), `db[].file` (schema). *Grep it; do not read it whole.*
3. **For the "why"** — grep `knowledge-graph/kg/nodes-sNN.jsonl` (219 fine-grained technique / principle / pattern / pitfall / tool nodes, each with its source lesson + section).
4. **For the "how"** — grep `knowledge-graph/kg-code/nodes/*.jsonl` (177 implementation nodes; each carries an exact source `path` and optional `symbols` to grep inside it).
5. **For a diagram** — grep `knowledge-graph/kg-img/catalog.jsonl` (280 diagrams whose visual content is extracted into a text `summary`). **Read the summary; it is the authoritative rendering.** Only vision-read the actual image in the rare case the exact pixels matter.
6. **Open only the referenced files**, then follow the edges (`implemented_by`, `demonstrates`, `persists_with`, `uses`, `illustrates`) to hop between theory, code, and schema.

Full walkthrough: `REPOS/AID4-SKILL/knowledge-graph/graph-guide.md`. Paths inside `graph.json` resolve against `meta.repoRoot` + `knowledgeRoot`/`codeRoot`.

**Operating notes that otherwise cost a cycle:**

- **Never run the graph builder at the vault root** — it would walk 1.3 GB of vendored third-party sources. Always target one corpus directory.
- **Rebuilding a graph is expensive on this Drive path (budget 5–9 minutes) and is not byte-stable** — the same unchanged sources yield a different community count run to run. This is *not* something to do casually mid-build; it is vault maintenance, not project work.
- **Subsystem/community names shift between rebuilds. Cite file paths and symbols — never a community name — as an identifier.**
- **Do not use the base to answer questions about your own project's code.** It answers *"how has this kind of thing been solved before"*, not *"what does this repo do"*.
- *(Historical: earlier revisions of this clause warned about a doubled `AID4-SKILL\AID4-SKILL\` folder and a stale `meta.repoRoot`. Both were fixed upstream on 2026-08-10 — the graph is at `REPOS\AID4-SKILL\knowledge-graph\` and `meta.repoRoot` is correct.)*

### Hard boundaries (these keep the clause consistent with the rest of STC)

1. **Reference, never law.** The base informs *authoring* — the decisions you make while writing `Logic.md`, and background technique/idiom lookups while implementing. It is **never** a source of truth that competes with the freeze. Where the base and `Logic.md`/Appendix B/C disagree, **the freeze wins.**
2. **It does not widen a step's reading order by itself.** `Railroad Template.md` §0 pins exactly what a Builder loads per step, and rail 4 makes "the spec seems to need more than this" a `CONTRACT-GAP` rather than a licence to go hunting. Finding a better approach in the base **mid-build** does not authorise deviating from the frozen contract — it is raised as a `CONTRACT-GAP` and resolved by an amendment, exactly like any other. (See the same clause in `Railroad Template.md` §0.)
3. **Never copy architecture wholesale.** These are reference implementations, and a queryable graph over someone else's architecture makes mirroring it *easier*, which is precisely the failure mode a frozen interface exists to prevent. The risk grew with the tooling, not shrank. Mine *concepts and techniques*; build against *your* contract.
4. **Cite what you took.** When a decision comes from the base, name the source file — "following `…/src/shared/result.ts`" is worth more than "a common pattern is…". A curated base's value is that its answers are traceable; an uncited borrowing is indistinguishable from an improvisation, and improvisation is what the rails forbid.
5. **Check licences before vendoring.** Reading a third-party repo under `Mentioned repos/` for understanding is one thing; copying its code into your project is a licensing decision — and any dependency it implies belongs in `dependency.md` / `requirements.<ext>` like every other, coordinated the way those files' rules say.
6. **It never becomes a project dependency.** Neither the vault, nor the skill, nor the graphs may appear in the built system's dependency ledger, build scripts, or CI. If a project cannot be built by someone without access to it, the clause has been misused.

---

## What was deliberately left generic

Anything that would have named a specific technology, library, storage engine, or domain concept was replaced with a placeholder or an instruction comment. What survives is the *pattern*: layered sections with a consistent internal shape (goal → mechanism → invariants → open questions), NORMATIVE appendices as the single source of truth for identifiers, a step template that leaves no interpretation room, and a stub-first build order. Fill the placeholders with your project's actual technology choices — the standard doesn't prescribe any.
